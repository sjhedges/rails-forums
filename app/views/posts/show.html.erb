<%= @post.title %>

      <table class="table table.hover">
          <tr>
              <th> Post Name </th>
          </tr>
          <% @post.comments.each do |post| %>
          <tr>
              <td> <%= post.text %> </td>
              <td><%= link_to 'Edit Comment', edit_category_post_path(@category, post), class: 'btn btn-warning' %></td>

          </tr>
          <% end %>
      </table>
